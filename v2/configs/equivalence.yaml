name: equivalence
seeds: [0]
data:
  cnn: 0                 # pinned CNN; run_equivalence also supports the v1 embeddings
  fit_split: train
  fit_samples: 20000
  eval_split: test
  eval_samples: null
models:
  - {name: iqc_static}
  - {name: iqc_fixed, params: {K: 3, eta: 0.1}}
backend: {kind: exact}
shots: null
noise: null
output_dir: v2/results/equivalence
